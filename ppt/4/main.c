#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	/*
	int a;
	a = 5;
	a = a * 2 + 10;
	printf("%d\n", a);
	a = 5;
	(a *= 2) + 10;
	printf("%d\n", a);
	*/
	
	/*
	int i = 0;
	while (++i <= 10) printf("++i %d\t", i);
	printf("\n");
	i = 0;
	while (i++ <= 10) printf("i++ %d\t", i);
	*/
	
	//비트 연산자부터 
	
	return 0;
}
